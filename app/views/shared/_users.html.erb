<% if @users.any? %>
  <ol class="users follow">
    <%= render @users %>
  </ol>
  <div class="flex-center">
    <%= will_paginate @users %>
  </div>
<% else %>
  <%= render 'shared/nothing' %>
<% end %>
