<% if @feed_items.any? %>
  <h3>Micropost Feed</h3>
  <!-- FLEX MICROPOSTS -->
  <div class="my-flex-container flex-column microposts-flex-container">
    <%= render @feed_items %>
  </div>
  <!-- FLEX MICROPOSTS -->
  <div class="text-center">
    <%= will_paginate @feed_items, params: { controller: 'static_pages', action: 'home' } %>
  </div>
<% end %>
