<nav class="navbar navbar-inverse navbar-fixed-top">

  <!-- Brand and toggle get grouped for better mobile display -->
  <div class="navbar-brand">
    <%= link_to 'Sample App', root_path , id: 'logo' %>
  </div>
  <button type="button" class="navbar-toggle collapsed my-navbar-toggler-mobile" data-toggle="collapse" data-target="#bs-navbar-collapse-1" aria-expanded="false">
    <span class="sr-only">Toggle navigation</span>
    <span class="icon-bar"></span>
    <span class="icon-bar"></span>
    <span class="icon-bar"></span>
  </button>

  <!-- Collect the nav links, forms, and other content for toggling -->
  <div class="collapse navbar-collapse my-navbar-collapse" id="bs-navbar-collapse-1">
    <ul class="nav navbar-nav navbar-right">
      <li><%= active_link_to 'Home', root_path, active: [static_pages: :home] %></li>
      <% if logged_in? %>
        <li><%= active_link_to 'Users', users_path, active: [users: :index] %></li>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown">
            Account
            <b class="caret"></b>
          </a>
          <ul class="dropdown-menu">
            <li><%= link_to 'Profile', current_user %></li>
            <li><%= link_to 'Settings', edit_user_path(current_user) %></li>
            <li class="divider"></li>
            <li><%= link_to 'Log out', logout_path, method: 'delete' %></li>
          </ul>
        </li>
      <% else %>
        <li><%= link_to 'Log in', login_path %></li>
      <% end %>
    </ul>
  </div>

</nav>
<!-- /.navbar-collapse -->
